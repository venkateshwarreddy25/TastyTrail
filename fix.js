const fs = require('fs');
let content = fs.readFileSync('src/pages/Dashboard.tsx', 'utf8');

// Find the broken ending of admin return and the start of floating cards
const target = `          </div>\r\n        )}\r\n      </div>\r\n        </div>\r\n      </div>\r\n\r\n      {/* ── 3 MASSIVE WHITE CARDS ── */}\n`;
const replacement = `          </div>\r\n        )}\r\n      </div>\r\n    );\r\n  }\r\n\r\n  /* ─── CUSTOMER VIEW ─── */\r\n  return (\r\n    <div style={{ width: '100vw', minHeight: '100vh', margin: 0, padding: 0, overflowX: 'hidden', backgroundColor: '#fff' }}>\r\n\r\n      {/* Active Orders Tracking Bar */}\r\n      {activeOrders.length > 0 && (\r\n        <div className="bg-[#fc8019] text-white px-4 py-3 z-40 shadow relative">\r\n          <div className="w-full max-w-7xl mx-auto flex items-center justify-between">\r\n            <div className="flex items-center gap-3">\r\n              <ClockIcon className="w-5 h-5 animate-pulse" />\r\n              <span className="font-semibold text-sm">{activeOrders.length} order(s) are being prepared!</span>\r\n            </div>\r\n            <Link to="/orders" className="bg-white text-[#fc8019] px-4 py-1.5 rounded text-xs font-bold uppercase hover:shadow-lg transition-shadow">Track</Link>\r\n          </div>\r\n        </div>\r\n      )}\r\n\r\n      {/* ── SWIGGY-STYLE HERO ── */}\r\n      <div style={{ width: '100%', background: '#FF5200', minHeight: '460px', position: 'relative', overflow: 'hidden', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', padding: '60px 20px', boxSizing: 'border-box' }}>\r\n        <img src="https://images.unsplash.com/photo-1512621776951-a57141f2eefd?w=400&q=80" alt="Vegetables" style={{ position: 'absolute', left: 0, bottom: 0, height: '420px', objectFit: 'cover', zIndex: 1, opacity: 0.8 }} className="hidden md:block pointer-events-none" />\r\n        <img src="https://images.unsplash.com/photo-1567620905732-2d1ec7ab7445?w=400&q=80" alt="Food Platter" style={{ position: 'absolute', right: 0, bottom: 0, height: '380px', objectFit: 'cover', zIndex: 1, opacity: 0.8 }} className="hidden md:block pointer-events-none" />\r\n        <div style={{ position: 'relative', zIndex: 2, width: '100%', maxWidth: '800px', textAlign: 'center' }}>\r\n          <h1 style={{ color: '#fff', fontSize: '48px', fontWeight: 800, lineHeight: 1.2, margin: '0 auto 32px auto', textAlign: 'center' }}>\r\n            Order food and groceries. Discover best restaurants. TastyTrail it!\r\n          </h1>\r\n          <div style={{ display: 'flex', flexDirection: 'row', gap: 0, maxWidth: '750px', width: '100%', margin: '0 auto', boxShadow: '0 4px 16px rgba(0,0,0,0.2)' }}>\r\n            <div style={{ flex: 1, backgroundColor: '#fff', height: '56px', borderTopLeftRadius: '8px', borderBottomLeftRadius: '8px', display: 'flex', alignItems: 'center', padding: '0 16px', position: 'relative', borderRight: '1px solid #eee' }}>\r\n              <span style={{ marginRight: '8px', fontSize: '18px' }}>📍</span>\r\n              <input ref={locSearchBarRef} type="text" placeholder="Enter your delivery location" value={typedLocation} onChange={handleLocationType} style={{ width: '100%', border: 'none', outline: 'none', background: 'transparent', fontSize: '15px', fontWeight: 600, color: '#333' }} />\r\n              <span style={{ color: '#999' }}>⌄</span>\r\n              {isSearchingLocation && (<div className="absolute right-10 top-1/2 -translate-y-1/2"><div className="w-4 h-4 border-2 border-[#ff5200]/30 border-t-[#ff5200] rounded-full animate-spin"></div></div>)}\r\n              {locationSuggestions.length > 0 && (\r\n                <div ref={locDropdownRef} className="absolute top-[60px] left-0 w-full bg-white shadow-2xl rounded-2xl z-50 max-h-[300px] overflow-y-auto mt-2 py-2 text-left">\r\n                  {locationSuggestions.map((sug, i) => (<div key={i} className="px-5 py-3 hover:bg-orange-50 cursor-pointer border-b border-gray-50 last:border-0 text-sm text-gray-700" onClick={() => fetchRestaurantsAt(parseFloat(sug.lat), parseFloat(sug.lon), sug.display_name.split(',')[0])}><strong>{sug.display_name.split(',')[0]}</strong><span className="text-gray-400 block text-xs truncate mt-0.5">{sug.display_name}</span></div>))}\r\n                </div>\r\n              )}\r\n            </div>\r\n            <div style={{ flex: 2, backgroundColor: '#fff', height: '56px', borderTopRightRadius: '8px', borderBottomRightRadius: '8px', display: 'flex', alignItems: 'center', padding: '0 16px', position: 'relative' }}>\r\n              <input ref={searchBarRef} type="text" value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} placeholder="Search for restaurant, item or more" style={{ width: '100%', border: 'none', outline: 'none', background: 'transparent', fontSize: '15px', fontWeight: 500, color: '#333' }} />\r\n              <span style={{ color: '#999', fontSize: '18px' }}>🔍</span>\r\n              {searchQuery.length > 0 && (\r\n                <div ref={foodDropdownRef} className="absolute top-[60px] left-0 w-full bg-white shadow-2xl rounded-2xl z-50 max-h-[300px] overflow-y-auto mt-2 py-2 text-left">\r\n                  {searchResults.length > 0 ? searchResults.map((item, index) => (<div key={index} onClick={() => handleSearchSelect(item)} className="px-5 py-4 flex items-center gap-4 hover:bg-orange-50 cursor-pointer border-b border-gray-50 last:border-0"><img src={item.photoUrl} alt={item.name} className="w-12 h-12 rounded-xl object-cover shadow-sm" /><div><p className="font-bold text-gray-800 text-sm">{item.name}</p><p className="text-xs text-gray-400">{item.category} · ₹{item.price}</p></div></div>)) : (<div className="p-6 text-gray-400 text-center text-sm">No results found</div>)}\r\n                </div>\r\n              )}\r\n            </div>\r\n          </div>\r\n        </div>\r\n      </div>\r\n\r\n      {/* ── 3 MASSIVE WHITE CARDS ── */}\n`;

if (content.includes(target)) {
  content = content.replace(target, replacement);
  fs.writeFileSync('src/pages/Dashboard.tsx', content, 'utf8');
  console.log('SUCCESS: Fixed admin return and added customer return with hero.');
} else {
  // Try without \r
  const target2 = `          </div>\n        )}\n      </div>\n        </div>\n      </div>\n\n      {/* ── 3 MASSIVE WHITE CARDS ── */}\n`;
  if (content.includes(target2)) {
    const repl2 = replacement.replace(/\r\n/g, '\n');
    content = content.replace(target2, repl2);
    fs.writeFileSync('src/pages/Dashboard.tsx', content, 'utf8');
    console.log('SUCCESS (LF): Fixed admin return and added customer return with hero.');
  } else {
    // Find exact positions
    const idx1 = content.indexOf('      </div>\n        </div>\n      </div>');
    const idx2 = content.indexOf('      {/* ── 3 MASSIVE WHITE CARDS ── */}');
    console.log('FAILED. idx1:', idx1, 'idx2:', idx2);
    // Print what is around idx2
    if (idx2 > 0) console.log('Context around cards:', JSON.stringify(content.substring(idx2-200, idx2+10)));
  }
}
